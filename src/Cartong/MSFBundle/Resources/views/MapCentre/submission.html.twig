{% extends "CartongMSFBundle:MapCentre:layout.html.twig" %}

{% block jumbotron %}
        <div class="jumbotron-main"></div>
        <div id="breadcrumb">
            <div class="container">
			 <span class="glyphicon glyphicon-home"></span> <small><small><span class="glyphicon glyphicon-play"></span></small></small> <a href="#">{{ 'Submission'|trans }}</a>
            </div>
        </div>
{% endblock %}

{% block content %}
{{ parent() }}
        <div id="">
            <div class="container">
                <div class="col-md-12">
                    <h1 class="page-header">{{'Send your data!'|trans }}</h1>
                    {% for flashMessage in app.session.flashbag.get('notice') %}
                        <div class="alert alert-success">
                            {{ flashMessage }}
                        </div>
                    {% endfor %}
                    {% for flashMessage in app.session.flashbag.get('error') %}
                        <div class="alert alert-danger">
                            {{ flashMessage }}
                        </div>
                    {% endfor %}
					</div>
				 <div class="col-md-8 intro">
				
					The Map Centre is a collaborative tool. 
					This platform is a sharing platform. All contributions are welcome. 
					Data, maps and other map products will be assessed by the GIS unit before being made available on the Map Centre.
					Thanks for your collaboration! 
			
                    <form role="form" enctype="multipart/form-data" action="{{ path('cartong_msf_mapcentre_postsubmission') }}" method="post">
                        <div class="form-group">
                            <label for="name">{{ 'Name'|trans }}</label>
                            <input type="text" name="name" class="form-control" id="name" placeholder="{{ 'Enter your name...'|trans }}" required="required"/>
                        </div>
                        <div class="form-group">
                            <label for="email">{{ 'Email address'|trans }}</label>
                            <input type="email" name="email" class="form-control" id="email" placeholder="{{ 'Enter your email...'|trans }}" required="required"/>
                        </div>
						 <div class="form-group">
                            <label for="comment">{{ 'Comments'|trans }}</label>
                            <input type="text" name="comment" class="form-control" id="comment" placeholder="{{ 'Please describe what you are sending ...'|trans }}" required="required"/>
                        </div>
                        <div class="form-group">
                            <label for="file">{{ 'Upload file'|trans }}</label>
                            <input type="file" name="file" id="file" required="required"/>
                        </div>
                        <button type="submit" class="btn btn-primary">{{ 'Send'|trans }}</button>
                    </form>
                </div>
				
				
								
				
				 <div class="col-md-3 col-md-offset-1">
				  <div class="col-md-12 wanted">
				  <img src="{{ asset('bundles/cartongmsf/images/wanted.png') }}" width="100%"/>
				  </div>
				  <div class="col-md-12 box wanted-box">
					We are particularly interested by: 
						<ul>
						<li>Health Areas</li>
						<li>Health Centres</li>
						<li>Interesting Field Maps</li>
						</ul>
					Any available format may be very useful:
						<ul>
						<li>PDF, Excel files, Word Files</li>
						<li>PPT files, pictures, schema</li>
						<li>Shapefiles, KML ...</li>
						</ul>
																
					</div>
				</div>
            </div>
        </div>
{% endblock %}
